#!/bin/bash

# =============================================
# AUTOINSTALADOR CORRIGIDO PARA BOT SSH
# Versão: 3.1 (Resolve conflitos de dependências)
# =============================================

# Cores no terminal
RED='\033[1;31m'
GREEN='\033[1;32m'
YELLOW='\033[1;33m'
BLUE='\033[1;34m'
NC='\033[0m'

# Função de erro melhorada
check_error() {
  if [ $? -ne 0 ]; then
    echo -e "${RED}❌ Erro no passo: $1${NC}"
    
    # Correção automática para erros comuns
    case "$1" in
      "Dependências Node.js")
        echo -e "${YELLOW}🔄 Tentando corrigir conflitos de dependências...${NC}"
        sudo apt remove --purge nodejs npm -y
        curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
        sudo apt-get install -y nodejs
        ;;
      *)
        exit 1
        ;;
    esac
    
    # Tentar novamente após correção
    return 0
  fi
}

# Verificação de segurança
echo -e "${RED}⚠️⚠️⚠️ ATENÇÃO! ESTE SCRIPT IRÁ: ⚠️⚠️⚠️${NC}"
echo -e "${RED}1. APAGAR TODOS OS DADOS DESTA VPS${NC}"
echo -e "${RED}2. INSTALAR DEBIAN 11 DO ZERO${NC}"
echo -e "${RED}3. CONFIGURAR O BOT SSH AUTOMATICAMENTE${NC}"
echo -e "\n${YELLOW}Você tem 30 segundos para cancelar (Ctrl+C)${NC}"
sleep 30

# Confirmação final
read -p "⚠️ Confirmar formatação completa e instalação? (s/N) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Ss]$ ]]; then
  echo -e "${GREEN}Operação cancelada pelo usuário.${NC}"
  exit 0
fi

# 1. Pré-formatação (backup opcional)
echo -e "${BLUE}🔄 Preparando ambiente para formatação...${NC}"
mkdir -p /tmp/backup-bot 2>/dev/null
cp -r ~/bot-ssh /tmp/backup-bot/ 2>/dev/null

# 2. Instalação limpa do Debian 11
echo -e "${BLUE}🛠️ Instalando Debian 11 (Bullseye)...${NC}"
apt-get purge -y * 2>/dev/null
apt-get autoremove -y 2>/dev/null

# Instalação mínima
apt-get update && apt-get install -y --reinstall debian-keyring debian-archive-keyring
check_error "Repositórios Debian"

# 3. Instalação do ambiente básico (com correção para Node.js)
echo -e "${BLUE}📦 Instalando ambiente básico...${NC}"
sudo apt remove --purge nodejs npm nodejs-legacy libnode72 -y 2>/dev/null
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y \
  sudo curl wget git unzip \
  build-essential python3 make gcc \
  libssh2-1-dev nodejs \
  net-tools iptables
check_error "Dependências básicas"

# 4. Instalação do Bot SSH
echo -e "${GREEN}🤖 Iniciando instalação do Bot SSH...${NC}"

# Configuração do diretório
mkdir -p ~/bot-ssh && cd ~/bot-ssh || check_error "Diretório"

# Baixar Bot
echo -e "${BLUE}⬇️ Baixando o Bot SSH...${NC}"
wget -q --show-progress https://github.com/Marcelo1408/BOT_SCRIPT_SSH/raw/main/novobotssh.zip -O bot.zip
check_error "Download do Bot"

unzip -o bot.zip
rm -f bot.zip
check_error "Extração"

# Limpar dependências antigas
rm -rf node_modules package-lock.json

# Criar package.json atualizado
echo -e "${BLUE}📄 Criando package.json...${NC}"
cat > package.json <<EOF
{
  "name": "bot-ssh",
  "version": "2.1",
  "dependencies": {
    "axios": "^1.10.0",
    "date-fns": "^2.30.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "fs-extra": "^11.2.0",
    "lodash": "^4.17.21",
    "multer": "^2.0.1",
    "node-cron": "^4.1.1",
    "node-ssh": "^13.2.1",
    "node-telegram-bot-api": "^0.61.0",
    "pm2": "^6.0.8",
    "ssh2": "^1.11.0",
    "ssh2-sftp-client": "^12.0.1"
  }
}
EOF

# Instalar dependências com força (--force)
echo -e "${BLUE}📦 Instalando dependências Node.js...${NC}"
npm install --force
check_error "Instalação de dependências"

# 5. Configuração do Bot
echo -e "${BLUE}⚙️ Configuração do Bot SSH...${NC}"
read -p "Digite o BOT_TOKEN do Telegram: " BOT_TOKEN
read -p "Digite o ADM_ID do Telegram: " ADM_ID

# Criar .env com configuração automática
cat > .env <<EOF
BOT_TOKEN=$BOT_TOKEN
ADM_ID=$ADM_ID
SERVER_HOST=$(curl -s ifconfig.me)
SERVER_USER=admin
SERVER_PASSWORD=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 16)
SERVER_PORT=22
SSH_TIMEOUT=20000
EOF

# 6. Gerenciamento com PM2
echo -e "${BLUE}🚀 Configurando PM2...${NC}"
npm install -g pm2
pm2 delete bot-ssh 2>/dev/null
pm2 start index.js --name "bot-ssh"
pm2 startup && pm2 save

# 7. Finalização
echo -e "${GREEN}"
echo "============================================="
echo "🎉 INSTALAÇÃO CONCLUÍDA COM SUCESSO!"
echo "============================================="
echo -e "${NC}"
echo -e "${BLUE}📌 INFORMAÇÕES IMPORTANTES:${NC}"
echo -e "IP do servidor: $(curl -s ifconfig.me)"
echo -e "Usuário SSH: admin"
echo -e "Senha SSH: $(grep SERVER_PASSWORD .env | cut -d= -f2 | tr -d "'")"
echo -e "\n${BLUE}⚙️ COMANDOS ÚTEIS:${NC}"
echo -e "pm2 logs bot-ssh       → Ver logs"
echo -e "pm2 restart bot-ssh    → Reiniciar bot"
echo -e "pm2 list               → Ver status"
echo -e "\n${YELLOW}⚠️ Configure um firewall adequado após a instalação!${NC}"
